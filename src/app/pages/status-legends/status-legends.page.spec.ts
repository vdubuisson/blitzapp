import StatusLegendsPage from './status-legends.page';

describe('StatusLegendsPage', () => {
  let page: StatusLegendsPage;

  beforeEach(() => {
    page = new StatusLegendsPage();
  });

  it('should create page', () => {
    expect(page).toBeTruthy();
  });
});
