import { VictoryNamePipe } from './victory-name.pipe';

describe('VictoryNamePipe', () => {
  it('create an instance', () => {
    const pipe = new VictoryNamePipe();
    expect(pipe).toBeTruthy();
  });
});
