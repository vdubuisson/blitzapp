import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fr.blitzapp.lgmj',
  appName: 'loup-garou-mj',
  webDir: 'dist/loup-garou-mj/browser',
};

export default config;
